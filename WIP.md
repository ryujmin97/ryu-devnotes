# WIP — 중단 지점 (체크포인트, 세션 종료 아님)

- 저장 시각: 2026-08-21 (24차 계속, 25차 착수 직전 — "이번
  실주행에서 가장 시급히 반영해야 할 것" 논의 시작, 사용자가
  "저장" 명시 요청)

## 25차 착수 — 사용자가 시급 이슈 2건 제기, 1번부터 진행 중

사용자가 이번 하루치 실주행(24차 배치 분석 대상과 동일 세트로
추정)에서 체감한 문제 2건을 신규 제보:

1. **[진행 중] 카메라 인식 시 미감속 + 레이더 락온 순간 급감속**
   — 22차에서 이미 근본원인 파악된 바로 그 증상(재제보). 사용자가
   **화면녹화 영상**을 업로드 예정(각 영상 마지막 20초 구간에
   증상 포착돼 있음) — **아직 미업로드, 다음 메시지에서 받을
   차례**. 업로드되면:
   - 어느 라우트(zip)의 영상인지 먼저 확인(사용자에게 물어봤음,
     아직 답 없음 — 24차에서 이미 분석한 15개 라우트 중 하나일
     가능성 높음, evidence/route_summaries_260821/에 요약 있음)
   - `extract_dashcam_frames.py`로 화면 클럭 오버레이 매칭 →
     rlog 프레임과 대조
   - 카메라 인식 시작~레이더 락온 시점 구간의 dRel/leadVRel/aEgo/
     `_vision_dRel_rate`(재계산 필요, publish 안 됨 —
     `work/sim_vision_rate.py` 시뮬레이터 재사용 가능, 23차에서
     작성했으나 devnotes 미편입 상태) 프레임 단위 대조
   - 곡선구간(`src=vturn`) 여부 확인 — 23차에서 발견된 "곡선
     노이즈로 인한 오탐성 급감속 신호"와 "진짜 원거리 접근
     TTC 게이팅 한계" 두 가지 메커니즘 중 어느 쪽인지 구분 필요

   **이미 파악된 근본원인(22~23차, 재확인 완료, 코드 미변경)**:
   a) `LEAD_ACQ_TTC_CAUTION=6.0s` 문턱이 원거리(dRel≈85~120m)에서
      물리적으로 도달 불가 — 카메라가 접근율을 정확히 감지해도
      거리가 멀면 TTC=dRel/rate 계산값이 문턱을 못 넘어 무시됨.
   b) 레이더 락온 순간 vRel 불연속 점프(재현 2건 모두 -8.0~-8.4m/s
      로 유사값 점프) — 단안 카메라 깊이추정이 곡선/원거리에서
      낙관적으로 보고.
   - **대안 3개 설계만 되어 있고 미착수**: 1)TTC 문턱 완화
     (6.0→10~12s), 2)closing-rate 절대값 게이트(-5.5~-6.0m/s),
     4)`_vision_dRel_rate`를 `v_lead`에 직접 반영(사용자 제안,
     가장 공격적). 3개 다 23차에서 발견한 곡선 노이즈 취약성
     때문에 오탐 증폭 위험 있어 **곡선 노이즈 필터링 선행 필요**
     하다는 게 결론.

2. **[대기 중] 옆차선 차량 락온 순간 급감속** — 아직 상세 논의
   전, 사용자가 "그 외 몇 개 더 있지만 1번부터"라고 해서 순서상
   대기. 1번 완료 후 착수 예정.

## 다음 세션(또는 다음 메시지)에서 이어갈 것
1. **사용자의 화면녹화 영상 업로드 대기** — 받는 즉시 위 분석
   방식대로 진행.
2. 라우트 특정(어느 zip인지) 확인.
3. 영상 분석 후 대안 1/2/4번 중 어느 것을 적용할지(또는 곡선 노이즈
   필터링을 먼저 할지) 사용자와 결정 → 패치 설계 → 패치 워크플로우
   (C:\dev\patch 경유 git am).
4. 1번 해결/패치 완료 후 2번(옆차선 락온 급감속) 착수.

## 다음 세션 우선 과제 (25차와 별개, 참고용, 순서 밀림)
1. 고속도로 급접근(harsh) 케이스 실측 표본 확보 — 24차까지 확보된
   b403d52 검증은 전부 "온건한 접근" 케이스뿐, 급접근 시나리오는
   미확보. (이번 25차의 영상 제보가 바로 이 급접근 표본이 될 가능성
   높음.)
2. route3(`dda0d533ce`)의 `vision_radar_crossover
   count_highway_est=0`이 route_summary.py 버그(route4에서 발견+
   수정) 영향인지 재확인(낮은 우선순위).
3. `source_pair_flicker` 관련 문서에서 경쟁 소스를 5종에서 최소
   7종(+bump/gas)으로 반영 필요.

## 다음 세션 시작 시
이 WIP.md에 "25차 착수" 섹션이 있으면 무조건 그 지점(영상 업로드
대기)부터 이어감. 사용자가 아직 영상을 안 올렸다면 다시 요청.
