# Claude 프로젝트 지침 (Project Instructions / Custom Instructions)

아래 내용을 어느 계정이든 Claude 프로젝트(claude.ai) 생성 시
"프로젝트 지침" / "Custom instructions" 칸에 그대로 붙여넣으면 된다.
**파일 업로드가 아니라 텍스트 지침**이라 계정을 새로 만들거나 프로젝트를
새로 파도 이 텍스트 한 번만 붙여넣으면 동일한 워크플로우가 재현된다.

**[2026-08-27 개정] 이 버전에서 바뀐 것 (94차 이후 세션에서 지침 드리프트
발견 → 수정):**
- GH_TOKEN 자동 push 절차 전면 삭제 — push는 항상 사용자가 로컬에서 직접
  수행한다(토큰을 대화/컨테이너에 두지 않는다는 원칙을 더 단순하게 강제).
- **WIP.md 회차 감지 방식 수정** — 실제 `WIP.md`는 `## 회차: N` 같은 별도
  필드를 쓴 적이 없고 처음부터 `## N차 (상태) — 제목` 헤더 하나로만 기록돼
  왔다. 예전 버전 지침에 있던 `grep "^## 회차"` 방식은 이 실제 형식과
  전혀 안 맞아서, 94차까지(103개 항목) 쌓인 WIP.md가 있는데도 세션
  시작 시 "WIP.md: 없음"으로 잘못 보고되는 사고가 있었다 — 아래 새 방식으로
  교체.
- **"주요 작업 종료 시마다 파일+PowerShell 제공" 원칙을 명시적 규칙으로
  승격** — 이전엔 "세션 종료"/"체크포인트 트리거" 시에만 파일을 주는
  것처럼 읽혔는데, 실제 목적(다음 세션이 이어받으려면 사람이 push해야
  함)을 생각하면 **devnotes 파일이 바뀌는 모든 작업 단위**가 대상이어야
  한다. 아래 "작업 결과물 전달 원칙" 절로 통합.

---

## 붙여넣을 지침 (여기부터)

이 프로젝트는 openpilot 포크(`ryujmin97/ryu`, Genesis DH 2015/2016 대상)의
종방향/횡방향 제어 로직 개선 작업이다. 주 요청 유형: 최신 커밋 분석, 실주행
로그 분석, 문제점/원인 파악, 대안 제시, 패치 적용.

**중요: 이 프로젝트는 파일 업로드(프로젝트 파일)를 쓰지 않는다.** 분석
기록/이슈 트래커/툴킷은 전부 GitHub 공개 레포 `ryujmin97/ryu-devnotes`에
있고, 세션마다 clone해서 쓴다. 세션이 끝나도 사람이 그 레포에 push해두면
다음 세션(같은 계정이든 다른 계정이든)이 항상 최신 상태를 이어받는다.
**GitHub push는 예외 없이 항상 사용자가 로컬(PowerShell)에서 직접 한다 —
Claude가 자동으로 push하는 경로는 없다. 토큰을 대화나 컨테이너에 절대
붙여넣지 않는다.**

**세션을 시작하면 다른 어떤 작업보다 먼저 아래를 그대로 실행한다:**

```bash
pip install --break-system-packages -q pycapnp zstandard
cd /home/claude
git clone --depth 1 https://github.com/ryujmin97/ryu-devnotes.git devnotes
git clone --branch c3-ms-dev --single-branch --depth 200 https://github.com/ryujmin97/ryu.git ryu
mkdir -p /home/claude/work
chmod +x /home/claude/devnotes/analyze_commits.sh
```

**clone 실패 시**: 1회 재시도 → 그래도 FAIL이면 임의로 진행하지 않고
사용자에게 즉시 보고 후 확인 요청 (브랜치명 변경/private 전환/네트워크
문제 등 원인 추정 포함).

**WIP.md 회차 확인 (반드시 이 방식 그대로 — 다른 패턴 쓰지 않는다):**
`WIP.md`에는 회차를 나타내는 별도 필드가 없다. 최상단 줄 자체가 회차
헤더다(`## 94차 (완료 — ...) — 제목` 형식). 따라서 존재/회차 파악은
아래처럼 한다:

```bash
[ -f /home/claude/devnotes/WIP.md ] && {
  echo "WIP.md 존재, 총 $(grep -c '^## ' /home/claude/devnotes/WIP.md)건"
  head -1 /home/claude/devnotes/WIP.md
} || echo "WIP.md: 없음"
```

절대로 `grep "^## 회차"` 같은 별도 필드를 찾지 않는다 — 그런 필드는 실제
파일에 존재한 적이 없고, 그 패턴을 쓰면 회차가 아무리 쌓여 있어도 항상
"없음"으로 오판된다(94차까지의 작업 이력을 통째로 놓칠 뻔한 사고 있었음).

실행 후 응답 맨 첫 줄에 고정 포맷으로 보고한다:
`[세션시작] devnotes: OK/FAIL, ryu: OK/FAIL, WIP.md: <head -1 결과 요약> (총 N건) / 없음`

## 지연 로딩 원칙 (토큰 절약)

세션 시작 시점엔 "무엇이 존재하는지 / 최신 회차가 뭔지"만 파악하고,
파일 전체 내용은 실제로 이번 작업에 필요할 때만 읽는다.

| 파일 | 언제 읽나 |
|---|---|
| WIP.md 전체 | 사용자가 "이어서 진행"을 확정했을 때만 |
| LAST_ANALYZED.md | 커밋/로그 분석 범위를 정할 때만 |
| FINDINGS.md | 새 이슈 기록 전 중복 확인 시 — grep으로 키워드 먼저 검색 후 필요 구간만 |
| PARAMS_REGISTRY.md | 튜닝 상수를 실제로 다룰 때만 |
| toolkit/README.md | 로그 분석/시뮬레이션/새 스크립트 작성이 필요한 요청일 때 (아래 "먼저 찾는다" 원칙 참고) |
| ryu 커밋 로그 | `git log --oneline -20` 등 짧은 포맷 우선, 상세는 필요한 커밋만 `git show` |

**[반드시 지킬 것] 로그분석/시뮬레이션/검증용 스크립트를 다루는 모든 요청에서
아래 2가지를 어기지 않는다 (과거 여러 세션이 이 지침 없이 매번 새로
스크립트를 작성해 시간을 낭비한 전례가 반복돼 이 문단을 지침 본문에
직접 명시함):**
1. **먼저 찾는다.** 뭔가를 계산/재현/검증해야 하면 코드를 새로 짜기
   전에 반드시 `/home/claude/devnotes/toolkit/README.md`를 먼저 읽고,
   같은 목적의 함수/스크립트가 이미 있는지 확인한다. 있으면 그걸
   그대로(또는 옵션만 조금 바꿔서) 재사용한다.
2. **새로 만들면 반드시 레포에 넣는다.** README를 확인했는데도 없는
   경우에만 새로 작성한다. 새로 작성한 스크립트/함수는 검증 상태와
   무관하게 그 자리에서 `/home/claude/devnotes/toolkit/`에 저장하고,
   `toolkit/README.md`에 섹션을, `toolkit/CHANGELOG.md`에 한 줄 요약을
   추가한다. `work/` 폴더에만 스크래치로 남겨두고 끝내지 않는다 —
   컨테이너는 세션마다 리셋되므로 `work/`에만 있으면 다음 세션에서
   그대로 사라져 똑같은 도구를 또 새로 작성하는 낭비가 반복된다.

## WIP.md 회차 표기 규칙

WIP.md는 각 회차를 맨 위에 다음 형식 헤더로 추가하는 방식으로 누적한다
(별도 메타 필드 없이 헤더 한 줄 자체가 회차/상태/제목을 모두 담음):

```
## N차 (상태 요약, 예: 완료 — 구현+검증+패치전달 완료) — 한줄 제목
```

- 새 회차는 파일 맨 위에 추가(최신이 위), 이전 회차 기록은 아래에 보존
- 같은 회차 안에서 컨테이너 재시작 등으로 이어갈 땐 `## N차 계속`,
  `## N차 계속2` 형식 사용
- 회차가 너무 길어져 파일이 비대해지면(현재 268KB, 103건) 오래된 회차를
  요약 1~2줄로 축약하는 것을 고려할 수 있으나, **강제 규칙은 아니다** —
  실제로 지금까지 한 번도 축약된 적이 없고 전체가 원문 그대로 남아있다.
  축약이 필요하다고 판단되면 사용자에게 먼저 제안하고 확인 후 진행한다
  (임의로 오래된 회차 내용을 지우거나 축약하지 않는다).

## 작업 결과물 전달 원칙 (반드시 — 세션 종료를 기다리지 않는다)

**이 프로젝트의 이어가기(다음 세션이 이전 작업을 인식하는 것)는 전적으로
"사람이 devnotes를 push했는가"에 달려 있다.** Claude는 자동 push 수단이
없으므로, **devnotes 안의 파일(WIP.md/FINDINGS.md/LAST_ANALYZED.md/
PARAMS_REGISTRY.md/toolkit/* 등)이 실제로 바뀌는 작업 단위가 끝날 때마다**
— 세션이 끝나는지 여부와 무관하게 — 그 즉시 아래를 전부 한다. 사용자가
"체크포인트"/"저장"이라고 말해야만 주는 게 아니라, **Claude가 스스로
판단해서 먼저 준다**:

1. 바뀐 devnotes 파일을 `/home/claude/devnotes/` 안에서 직접 수정한다.
2. 바뀐 파일만 `/mnt/user-data/outputs/`에 생성 → `present_files`로
   전달한다. ("나중에 정리해서 드릴게요"처럼 미루지 않는다 — 그 턴에서
   바로 준다.)
3. **아래 PowerShell 명령어를 매번 그대로 같이 준다** (사용자가 요청하지
   않아도 항상 포함):
   ```powershell
   cd C:\dev\ryu-devnotes
   # 다운로드한 갱신 파일들을 이 폴더에 덮어쓴 뒤
   git add -A
   git commit -m "<한줄 요약>"
   git push
   ```
4. 코드(ryu) 변경이 있었으면 패치 파일도 같이 만든다:
   - `/mnt/user-data/outputs/`에 별도 생성
   - **패치 파일명 규칙**: `NNNN-짧은-설명.patch` (예:
     `0001-fix-lateral-gain.patch`), NNNN은 세션 내 4자리 순번
   - 저장 위치는 항상 `C:\dev\patch\`
   - 적용 안내(`git am` 등)도 같이 준다
   - ryu는 devnotes와 완전 별개 절차 — 커밋 메시지/폴더/명령어를 섞지 않음
5. 지금 준 것이 "세션의 완전한 종료"인지 "작업 중간 저장(체크포인트)"인지
   한 줄로 명확히 알린다.

**"작업 단위가 끝났다"의 기준** (아래 중 하나라도 해당하면 트리거):
- 사용자가 "체크포인트"/"저장"/"여기까지"/"끝났어" 등으로 명시 요청
- 패치 하나의 구현/검증이 한 단계 완료된 시점 (다음 단계로 넘어가기 전)
- 분석/조사 요청 하나가 결론까지 완료돼 FINDINGS.md 등에 기록할 내용이
  생긴 시점
- devnotes 지침/문서 자체를 수정한 시점(이번 대화처럼)
- 대화가 비정상적으로 길어짐(long_conversation_reminder 등)
- 위 조건에 해당하는지 애매하면, "이 정도 결과면 사람이 잃어버려도
  아까운가?"를 기준으로 판단한다 — 아까우면 바로 준다.

WIP.md에 기록할 때는 새 회차(`## N차 (체크포인트 — ...)` 또는 완료 시
`## N차 (완료 — ...)`)를 맨 위에 추가한다(누적, 이전 회차 보존).

## CSV 등 대용량 산출물 보관 (레포에 커밋 금지)

`extract_log.py`로 뽑은 route CSV 등은 **레포(devnotes)에는 절대
커밋하지 않는다** — git 히스토리는 삭제 커밋으로도 blob이 영구히 남아
레포가 계속 비대해진다. 재사용 가치가 있는 CSV는 사용자의 Google Drive
(연결돼 있다면)에 올려서 보관하고, `LAST_ANALYZED.md`/`FINDINGS.md`
해당 세션 기록에 `drive_csv: <fileId>` 형태로 남긴다. Drive 커넥터가
없으면 이 단계는 생략하고 `work/`에 스크래치로만 둔다(컨테이너 리셋 시
소실 — 재사용 필요하면 사용자에게 미리 알린다).

## 기타

- 모든 답변은 한글로.
- git 패치 파일은 항상 `C:\dev\patch\`에 저장.
- GitHub 인증/push는 항상 사용자 로컬 환경에서 처리. 토큰을 대화에
  붙여넣지 않음 (자동 push 경로 자체가 없음).

## 붙여넣을 지침 (여기까지)

---

## 계정별로 다르게 채워야 하는 부분
없음. 현재는 레포 URL이 public이라 별도 인증 없이 clone 가능. private으로
바뀌면 인증 절차를 이 지침 위쪽에 추가해야 한다 (단, GH_TOKEN을 지침
텍스트에 직접 넣는 방식은 쓰지 않는다 — 위 개정 사유 참고).
